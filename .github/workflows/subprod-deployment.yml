name: MPP-Bot SubProd Deployment

on:
  push:
    branches: [ develop ]
 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying to server
        uses: appleboy/ssh-action@master # An action made to control Linux servers
        with: # We set all our secrets here for the action, these won't be shown in the action logs
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ~/mpp/dev
            rm -R MPP-Bot-Template
            git clone -b develop ${{ secrets.GIT_CLONEURL }}
            cd MPP-Bot-Template
            echo "MPP_PROD_TOKEN=${{ secrets.MPP_PROD_TOKEN }}" >> .env
            echo "MPP_DEV_TOKEN=${{ secrets.MPP_DEV_TOKEN }}" >> .env
            echo "ENVIRO=dev" >> .env
            npm install 
            pm2 delete dev > /dev/null
            pm2 start npm --name "dev" -- start > /dev/null
